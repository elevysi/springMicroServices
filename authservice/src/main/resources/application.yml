server:
  port: 2000
  servlet:
#    context-path: /authservice
    session:
      cookie:
        name: TUTORIALAUTHSESSION
    
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authdb
    username: root
    password: root
  application:
    name: authservice
    
  security:
    oauth2:
      client:
        registration:
          facebook:
            client-id: 306620526617980
            client-secret: 7bfd7b12b764f30aa0da6db19d8cdf68
#            client-name: Facebook user
            provider: facebook
            scope:
                - email
                - public_profile
#            redirect-uri-template: http://localhost:2000/login/oauth2/code/facebook
            redirect-uri-template: http://localhost:2000/oauth2/callback/{registrationId}
        provider:
          facebook:
            authorizationUri: https://www.facebook.com/v3.0/dialog/oauth
            tokenUri: https://graph.facebook.com/v3.0/oauth/access_token
            userInfoUri: https://graph.facebook.com/v3.0/me?fields=id,first_name,middle_name,last_name,name,email,verified,is_verified,picture.width(250).height(250)

security:
  oauth2:
    client:
      clientId: auth
      clientSecret: root
      scope:
        read
      accessTokenUri: http://localhost:2000/oauth/token
      userAuthorizationUri: http://localhost:2000/oauth/authorize
    resource:
      userInfoUri: http://localhost:2000/api/user    
    
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
